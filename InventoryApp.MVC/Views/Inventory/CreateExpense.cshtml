@model InventoryApp.Core.Models.OperatingExpense
@{
    ViewData["Title"] = "Add Expense";
}

<div class="container-fluid m-4">
    <h2>@ViewData["Title"]</h2>

    <form asp-action="CreateExpense" method="post" class="row g-3 needs-validation">
        <div asp-validation-summary="All" class="text-danger"></div>
        @Html.AntiForgeryToken()

        <div class="form-group col-12 col-md-6">
            <label asp-for="Date"></label>
            <input asp-for="Date" type="date" class="form-control" required />
            <small class="form-text text-muted">Select the date of the expense.</small>
            <span asp-validation-for="Date" class="text-danger"></span>
        </div>

        <div class="form-group col-12 col-md-6">
            <label asp-for="Category"></label>
            <select asp-for="Category" class="form-select" asp-items="Html.GetEnumSelectList<ExpenseCategory>()" required>
                <option value="">-- Select Category --</option>
            </select>
            <small class="form-text text-muted">Choose the category that best describes this expense.</small>
            <span asp-validation-for="Category" class="text-danger"></span>
        </div>

        <div class="form-group col-12 col-md-6">
            <label asp-for="Description"></label>
            <input asp-for="Description" class="form-control" required placeholder="e.g. Electric Bill"/>
            <small class="form-text text-muted">Provide a short description of the expense.</small>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>

        <div class="form-group col-12 col-md-6">
            <label asp-for="Amount"></label>
            <input asp-for="Amount" type="number" step="0.01" class="form-control" required />
            <small class="form-text text-muted">Enter the total amount for this expense.</small>
            <span asp-validation-for="Amount" class="text-danger"></span>
        </div>

        <div class="form-group col-12 col-md-6">
            <label asp-for="Notes"></label>
            <textarea asp-for="Notes" class="form-control" rows="3" placeholder="Optional additional details"></textarea>
            <small class="form-text text-muted">Add any extra notes or relevant details (optional).</small>
            <span asp-validation-for="Notes" class="text-danger"></span>
        </div>


        <div class="col-12 d-flex justify-content-end mt-3">
            <a asp-action="OperatingExpenses" class="btn btn-secondary me-2">Cancel</a>
            <button type="submit" class="btn btn-success px-4">Save Expense</button>
        </div>

    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
